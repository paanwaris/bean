# The predict() method for bean_ellipsoid objects was removed in bean 0.2.1.
# It will return in a future release once the nicheR package (which provides
# the canonical implementation) is available on CRAN.
#
# This file is intentionally empty; delete it before running
# devtools::document().
